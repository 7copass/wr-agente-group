# PENDÊNCIA: hoje a conta 12 só tem 3 usuários, todos administradores, e nenhum
# é vendedor. Quando o Wallace criar os vendedores no Chatwoot, listar os ids aqui.
handoff:
  responsavel_padrao: 105       # Wallace — provisório
  rodizio: []                   # ids dos vendedores; vazio = usa o padrão
  sla_redistribuir_min: 5
  sla_reassumir_min: 10
  # Número que recebe uma mensagem de WhatsApp (pela mesma ponte já usada pelo Chatwoot,
  # hoje o quepasa) assim que um lead é qualificado. Precisa já existir uma conversa com
  # esse número no Chatwoot (troque uma mensagem com o Alex uma vez) — o agente não cria
  # conversa nova, para não arriscar mandar a notificação pro número errado.
  notificar_numero: "93984009798"

expediente:
  timezone: America/Sao_Paulo
  dias_uteis: [1, 2, 3, 4, 5]
  abre: "09:00"
  fecha: "17:00"

followup:
  janela_inicio: "08:00"
  janela_fim: "20:00"
  # Só para quem já respondeu ao menos uma vez (decisão do usuário).
  tentativas_min: [120, 1440, 5760, 10080]
